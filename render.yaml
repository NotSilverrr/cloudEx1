services:
  - type: web
    name: myweb
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: dbName
        fromDatabase:
          name: dbName
          property: database
      - key: dbUser
        fromDatabase:
          name: dbUser
          property: user
      - key: dbPassword
        fromDatabase:
          name: dbPassword
          property: password
      - key: hostName
        fromDatabase:
          name: hostName
          property: host
      
databases:
  - name: mydb
    databaseName: mydbname
    user: myuser
    plan: free